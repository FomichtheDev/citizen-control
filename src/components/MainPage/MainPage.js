const MainPage = () => {
  return (
    <div
      style={{
        width: "100%",
        height: "100%",
        display: "flex",
        justifyContent: "center",
        marginTop: "10%",
      }}
    >
      <h1>For getting full access you should sign in</h1>
    </div>
  );
};

export default MainPage;
